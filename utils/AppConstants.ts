export const rupeeSym = '₹';
const imageUrl = 'https://usa-cricket.s3.us-west-1.amazonaws.com/staticIcons';
export const images = {
    home: `${imageUrl}/home.png`,
    graph: `${imageUrl}/graph.png`,
    user: `${imageUrl}/user.png`
}